# Copy this file to .env and fill in real values. NEVER commit .env to git.

# Postgres connection string from your cloud provider (Neon / Supabase / RDS / Render Postgres)
DATABASE_URL=postgres://user:password@host:5432/worktrack?sslmode=require

# Long random secret used to sign JWTs. Generate with: openssl rand -base64 48
JWT_SECRET=replace-this-with-a-long-random-string

# How long a login session stays valid
JWT_EXPIRES_IN=8h

# Comma-separated list of frontend origins allowed to call this API
CORS_ORIGIN=http://localhost:5173,https://your-frontend-domain.com

# Port the API listens on (hosting platforms usually inject this automatically)
PORT=4000

# Used to seed the very first admin account (scripts/seed.js)
ADMIN_EMAIL=admin@yourcompany.com
ADMIN_PASSWORD=ChangeMe_Immediately123!
